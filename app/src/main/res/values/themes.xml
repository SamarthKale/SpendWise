<resources>
    <!-- Compose draws the real theme; this only styles the window before Compose starts. -->
    <style name="Theme.SpendWise" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
