package com.mj.spendwise.ui.screens.map

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.mj.spendwise.ui.components.PlaceholderScreen

/** OpenStreetMap (osmdroid) map arrives in Phase 6. */
@Composable
fun MapScreen(modifier: Modifier = Modifier) {
    PlaceholderScreen("Map", "OpenStreetMap (osmdroid) + navigation arrive in Phase 6.", modifier)
}
